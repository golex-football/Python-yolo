syntax = "proto3";

package golex.virtualtracking.model;

import "model/protobuf/common.proto";

message MaskFrame {
  uint32 width = 1;
  uint32 height = 2;
  PixelFormats pixel_format = 3;  // "GRAY8"
  bytes frame_data = 4;  // width * height
}
